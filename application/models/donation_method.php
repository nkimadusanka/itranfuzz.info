<?php if ( ! defined('BASEPATH')) exit('No direct script access allowed');

class Donation_method extends CI_model{
	var dmId;
	var description;
	var minAge;
	var maxAge;
	var weight;
	var dParticipating;
	var maxAmount;
}